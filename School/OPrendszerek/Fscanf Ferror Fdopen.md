#include "myinclude.h" /* példa a fdopen() használatára */ #define MAX 1024 /* puffer hossza az olvasáshoz */ int main() { int pfd[2]; /* a csővezeték azonosítói */ if (pipe(pfd) < 0) { syserr("pipe"); } pid_t pid; if ((pid = fork()) < 0) { syserr("fork"); } // fiú folyamat: if (pid == 0) { if (close(pfd[0]) < 0) { /* írni fog, az olvasó felét lezárja */ syserr("close"); } FILE* fpw; // FILE pointer a cső olvasható írható végére if ((fpw = fdopen(pfd[1], "w")) == NULL) { syserr("fdopen"); } char a[] = "Helló!\n"; // ezt a kis sztringet írjuk át a csövön if (fprintf(fpw, "%s", a) < 0) { // fprintf hiba ha < 0 syserr("fprintf"); } if (fprintf(fpw, "%s", a) < 0) { // 2x írjuk át syserr("fprintf"); } /* a csövet használat után le kell zárni, ekkor tudja meg az olvasó folyamat, hogy vége az adat átvitelnek */ 6 Operációs rendszerek II. labor 2024 if (fclose(fpw) == EOF) { syserr("close1"); } exit(EXIT_SUCCESS); } else { // apa folyamat: if (close(pfd[1]) < 0) { // olvas, tehát bezárja az írható felét syserr("close"); } FILE* fpr; // FILE pointer a cső olvasható végére if ((fpr = fdopen(pfd[0], "r")) == NULL) { syserr("fdopen"); } char b[MAX]; // puffer while (fscanf(fpr, "%s", b) == 1) printf("csővezetékből: %s\n", b); } if (ferror(fpr)) { syserr("fpr"); } if (fclose(fpr) == EOF) { syserr("close2"); } wait(NULL); /* fiúra vár */ exit(EXIT_SUCCESS); } }